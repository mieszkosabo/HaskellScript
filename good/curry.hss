times :: Int -> Int -> Int
const times = \a, b => (
  a * b
);

timesTwo :: Int -> Int
const timesTwo = times(2);

print(timesTwo(5)); // 10

add :: Int -> Int -> Int
const add = \a, b => (a + b);

sum :: [Int] -> Int
const sum = reduce(add, 0);

print(sum(range(10))); // 55