times :: Int -> Int -> Int
const times = \a, b => (
  a * b
);

timesTwo :: Int -> Int
const timesTwo = times(2);

print(timesTwo(5)); // 10

sum :: [Int] -> Int
const sum = reduce(\acc, val => (acc + val), 0);

print(sum(range(10))); // 55