const factorial = \n => {
  if (n < 2) {
    return 1;
  }
  else {
    return n * factorial(n - 1);
  }
};


print(factorial(5)); // 120

const factorial' = \n => (
  n < 2 ? 1 : n * factorial'(n - 1)
);

print(factorial'(5)); // 120